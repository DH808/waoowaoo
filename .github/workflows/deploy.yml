name: Deploy waoowaoo to Tencent Cloud

on:
  push:
    branches: ["master"]
  workflow_dispatch: {}

concurrency:
  group: deploy-waoowaoo
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          script_stop: true
          command_timeout: 40m
          script: |
            set -euo pipefail
            cd /home/ubuntu/.openclaw/workspace/waoowaoo
            git fetch --all
            # Server repo origin points to upstream (waoowaooAI/waoowaoo) whose default branch is main.
            # Our fork remote is named dh808.
            git fetch dh808 master
            git reset --hard dh808/master
            docker compose pull || true
            # Build can occasionally be slow; allow longer time.
            export COMPOSE_DOCKER_CLI_BUILD=1
            export DOCKER_BUILDKIT=1
            timeout 30m docker compose up -d --build
            echo "Health check..."
            curl -fsS -I http://127.0.0.1:13000/ | head
